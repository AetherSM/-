spring:
  application:
    name: spring-boot-demo
 #    数据源配置
  datasource:
      url: jdbc:mysql://localhost:3306/college_student?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
      username: root
      password: yrf246810
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
        maximum-pool-size: 10
        minimum-idle: 2
        pool-name: SchoolRunHikariPool

# MyBatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.pojo.entity
  configuration:
    map-underscore-to-camel-case: true

# JWT配置
jwt:
  secret: mySecretKey123456789012345678901234567890  # JWT密钥（至少32位）
  expiration: 86400000  # token过期时间（毫秒），默认24小时

   #日志级别
logging:
  level:
    com.example.demo: DEBUG