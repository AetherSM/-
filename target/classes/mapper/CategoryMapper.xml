<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.example.demo.pojo.entity.Category">
        <id column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="icon" property="icon"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="listByParent" resultMap="BaseResultMap">
        select category_id, category_name, parent_id, sort_order, icon, status, created_at
        from categories
        <where>
            <if test="parentId != null"> parent_id = #{parentId} </if>
        </where>
        order by sort_order asc, category_id asc
    </select>

    <select id="findById" resultMap="BaseResultMap">
        select category_id, category_name, parent_id, sort_order, icon, status, created_at
        from categories
        where category_id = #{categoryId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="categoryId" keyColumn="category_id">
        insert into categories(category_name, parent_id, sort_order, icon, status, created_at)
        values(#{categoryName}, #{parentId}, #{sortOrder}, #{icon}, #{status}, now())
    </insert>

    <update id="update">
        update categories
        set category_name = #{categoryName},
            parent_id = #{parentId},
            sort_order = #{sortOrder},
            icon = #{icon},
            status = #{status}
        where category_id = #{categoryId}
    </update>

    <update id="updateStatus">
        update categories set status = #{status} where category_id = #{categoryId}
    </update>

</mapper>
